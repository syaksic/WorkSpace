from utils import *

if __name__ == "__main__":
	count=len(sys.argv)
	if count>1:
		if count==3:
			if sys.argv[1]=='syaksic':
				if sys.argv[1]=='798uoijlk':
					while True:
						reload(run)
		if count==2:
			if sys.argv[1]=='Hist':
				count=0
				while True:
					count+=1
					printTok(load_tok('Tok'))
					time.sleep(1)
		
		if count==2:
			if sys.argv[1]=='yak':
				import utils.yak
				while True:
					reload(utils.yak)


